# 书摘图片导出器 — 需求分析与技术决策

> 本地离线的「书摘 → 分享卡片图」生成器。输入 = 一段文字 + 元数据，输出 = 像素级 PNG。
> 无服务端逻辑，难点集中在**排版渲染**与**导出保真**两处。

## 素材定位（复刻对象）

| 素材 | 真实身份 | 作用 |
|---|---|---|
| `design/params_items/` | 微信读书 App 原生分享面板截图 | 主题/字体/背景参数来源 |
| `design/export_items/` | 微信读书导出成品图 | 要对齐的最终效果 |
| `design/app.webp` | 已上线 web 竞品 `ifenxiang.xyz` | 参考并超越；其登录/账号本地 app 直接砍掉 |

## 已锁定决策

- **外壳**：Tauri 2。CLI 先以 `signingIdentity:"-"` 生成 `.app` 中间态，`script/package_macos.sh` 再将 designated requirement 固定为 bundle id 并封装 DMG；避免裸 ad-hoc 的 DR 绑定 cdhash、重建后被 macOS 当成新 App。该方案只解决本机自用的身份连续性，不认证发布者；若未来引入 TCC/登录项权限或正式分发，必须切换 Developer ID（或持久自签名证书）并将证书纳入 DR。
- **应用图标**：深靛蓝胶囊承载暖象牙书页与朱橙双引号，三块大形分别表达应用容器、书摘与分享卡片；1024px sRGB RGBA 母版派生 ICNS，在 16px 与明暗 Dock 下保持可辨。
- **前端**：Vite + React 19 + Tailwind 4 + shadcn/Radix（与 squoosh 同款）
- **状态**：jotai + jotai-location（URL hash 同步，白拿"记住上次配置"+"配置分享链接"，即 ray.so `#theme=...&width=...` 的机制）
- **前端与外壳解耦**：同一份 Vite 产物既可 Tauri 打成 .app，也可当静态站（见下方字体代价）
- **字体策略（拿来主义）**：Mac 系统字体(中文六档+西文五档，见下方字体映射)为免费基线，够用即用，运行时探测在位与否；哪个主题用开源字体(思源系/霞鹜文楷，均可分发)更佳就补上。原则：**能拿到、好看、合规的就用，不定死系统**
- **不用 Next.js**：SSR 对本地工具无意义，Vite 更轻且与 squoosh 一致；只借 ray.so 的客户端渲染打法
- **外观三轴解耦**：原"主题"把版式与卡片色焊死(选经典即锁深色)，不合理。拆成 **版式×配色×图片背景** 三轴正交：版式(5, 经典置首, 仅结构=显隐/排布/内边距/字号, `data-layout`)/配色(12 套卡片色+自定义, 色相明度拉开无重复, 令牌 `--c-*` 由 App 写入、CSS 只消费)/图片背景(外框, 复刻 ray.so 14 主题渐变, 下拉+搜索)。配色**每版式各记一份**(`schemeByLayout`)，默认统一为首色 `ink`，可任意切换且各自记忆；一套配色只定 bg/ink/dark、余色按 ink 透明度阶梯派生(同源和谐)，自定义底色按明暗自动配可读文字。**内容元素显隐同理逐版式各记**(头像/昵称/日期/书名/章节/作者/平台/二维码, `showXByLayout`)——值全局共享，只有显示与否按版式区分，收进 Dock 底部可折叠「版式专属设置」(内容固定高度、超出内部滚动)。章节挂书名开关旁，书名关闭时章节强制级联隐藏。正文字号(`quoteFontSize`)全局统一直选 16/18/20/22px，不再逐版式区分、不再自适应。存储 key `excerpt.style.v9`(头像库另存 `excerpt.avatar.v1`)。事实源：`themes/{themes,schemes,backgrounds}.ts`。

## 设计语言（借鉴 ray.so）

**参考角色分工（别混）**
- `app.webp`(ifenxiang) = **功能蓝图**：有哪些字段/参数。视觉过时，不抄样子。
- ray.so = **视觉与交互蓝图**：居中画布 + 悬浮磨砂控制条范式。偷范式。
- `export_items`(微信读书) = **成品保真基准**：卡片内部还原到这个程度。

**范式**：作品即主角，控件隐退。居中卡片浮在渐变底板上占满舞台；样式参数收进底部磨砂悬浮条；导出在右上分裂按钮。**砍掉 ifenxiang 的表单双栏。**

**ray.so → 书摘 app 映射**

| ray.so | 书摘 app |
|---|---|
| 底部磨砂控制条 | 右侧磨砂设置栏(竖排右移、可收起)：版式/配色/字体/字号/背景(有背景开关+图片背景下拉合一行)/留白(随有背景联动)/倍率 + 底部可折叠「版式专属设置」(头像/昵称/日期/书名+章节/作者/平台/二维码，值共享、显隐逐版式，内容固定高度内部滚动) |
| Background 渐变底板 | 图片背景，直接复刻 ray.so 14 套预设主题(Midnight/Sunset/Candy… 命名+色点+搜索) |
| Padding 16/32/64/128 | 有背景模式的外框留白厚度，✅ 已接入 `styleAtom.framePadding`，Dock「留白」四档写 `--frame-pad` |
| 卡片侧边拖拽手柄 | 卡片宽度 |
| 右上 Export 分裂按钮 | 导出 PNG / 复制剪贴板 / 换倍率 |
| 代码直接敲在卡片 | 所有内容就地编辑（见下） |

**就地编辑（干掉表单）**：卡片元素点选即改——点正文改摘录、可见时点书名/作者/章节改字、点头像换图。头像/日期/书名/章节/作者/平台/二维码的**显隐**统一收进底部可折叠「版式专属设置」，逐版式各记一份；值(含二维码 url)仍全局共享，日期为派生展示、只在专属设置改，不就地编辑。

## 字体映射（macOS 基线，决策已变）

早期版本模拟微信读书原字体(兰亭黑/思源宋/仓耳今楷)三档，用营销名包一层系统字体——名不副实(思源宋并非真思源宋体，是系统宋体)。现改为**直接暴露 macOS 系统字体真名**，不再假装对应哪个原读字体：中文六档(苹方/宋体/黑体/楷体/仿宋/圆体) + 西文五档(San Francisco/Helvetica Neue/Times New Roman/Georgia/Avenir Next)，事实源 `themes/themes.ts` 的 `FONTS`，`state/useAvailableFonts` 运行时探测每档是否真的装了(canvas measureText 测宽对比法——`document.fonts.check()` 对本地系统字体不可靠，实测传一个完全不存在的字体名也返回 true，已弃用)，探测不到即从 Dock 的字体下拉里隐藏。默认苹方——三者里唯一原生为中英混排设计的字体(其拉丁字形是 Apple 按 San Francisco 的调子统一设计的)；宋体/楷体的拉丁字形只是系统自带、非精心适配，摘录常混英文时观感会打折扣。**New York 未登记**：原计划作为宋体的西文搭档，但实测和 San Francisco 一样受 WebKit 限制、无法按字面名寻址，且没有 `-apple-system` 那样的关键字替代通路，选了也渲染不出——属于死档位，不是"探测隐藏"而是干脆不登记。

- San Francisco 特殊：WebKit 出于隐私限制不允许按字面名 `"San Francisco"` 匹配，只能靠 `-apple-system` 关键字取用，故不设 probe、恒视为可用。
- **代价**：纯系统字体时，静态站在非 Mac 浏览器会因字体缺失而走样(探测机制会把缺失档位自动隐藏，不会显示错字，但可选字体会变少)。目标是 Mac app，基线已够；跨端一致需要时再上 @font-face 加载开源字体。

## 渲染核心（DOM/布局 → PNG）

- **MVP：html-to-image**。系统字体绕过了它最大的翻车点（CJK web 字体 inline），在 WKWebView 上更稳。
- **回退：Canvas 2D 手写排版**（换行/自适应字号/圆角/渐变全手写，但零 WebView 依赖）。
- **Satori 出局**：纯 JS 排版看不到系统字体，与"用系统字体"前提对立。
- **DIP**：渲染藏在 `Renderer` 接口后，MVP 与回退都吃 WKWebView 原生字体，一套道。

## 关键风险（未知的未知）

1. **WKWebView 保真**：Tauri 在 macOS 用 WebKit 非 Chromium。**Phase 0 已实测**：苹方/思源宋/渐变/二维码/圆角/文字经 html-to-image 4× 导出保真良好，字体风险退役。**但实测出一处 WebKit 限制**：html-to-image 渲染 `box-shadow` 会偏成单侧暗晕（像素采样实证：卡片右缘 lum 74 vs 左缘 145，本应对称），导出不可靠——已弃用卡片 box-shadow，改靠圆角+色彩对比区分。**推论**：`backdrop-filter` 等同类合成效果在导出中也应谨慎/先实测，勿默认可用。**又一坑(Phase 1 实测)**：被截节点的 `margin:auto` 会被 html-to-image 连同解析出的实际边距(宽窗口下达 294px)一起克隆，使导出整体右移、右侧被裁——**居中必须交父级 `justify/align: safe center`，被截节点自身 margin 必须为 0**。教训：导出对不上时先量成品像素/computed margin，别凭布局尺寸臆断。Canvas 兜底暂不需要。
2. **正文字号（决策已变）**：微信读书二分收缩字号填满卡片这套自适应算法，Phase 2 曾实现(`components/useAutoFit.ts`，测真实 `.quote` 节点二分求最大缩放比)，现已整体移除——长摘录下「卡片继续变高」比「字号被压缩变形」更符合书摘卡片的直觉阅读体验。改为 Dock「字号」全局直选 16/18/20/22px(`styleAtom.quoteFontSize`)，全版式统一，App 写入 `--quote-max` 直接落地 `font-size`，无中间推导。
3. **动态高度**：长文卡片变高，导出抓完整 `scrollHeight`，勿被视口裁剪。
4. **导出保真**：等 `document.fonts.ready` 再截；头像/图片转 base64 内嵌；html-to-image 首帧偶发丢字体（预热或截两次）。

## 数据模型（jotai atoms 草图）

```ts
ContentAtom  { content, bookTitle, chapter?, author, qrUrl, avatar?, shareName, shareDate }
StyleAtom    { theme, font, background: Color|Gradient, showQr, showFrame }   // 挂 jotai-location
ExportAtom   { scale: 2|4|6, format: 'png'|'webp'|'jpeg' }                    // 挂 jotai-location
```

## 主题矩阵

> v2 已三轴解耦：下表"版式特征"列的**结构**归入版式(`data-layout`)，**卡片颜色**抽成 6 套配色预设(令牌 `--c-*`)、
> 与版式正交可任意组合；"深底/浅底/粉底"等仅是各版式的**默认配色**(沿用原样张色)，非绑定。图片背景另走 ray.so 14 主题。

| 版式 | 结构特征（颜色见配色预设） | 素材状态 |
|---|---|---|
| 日历 | 大日期数字+月份+星期·居中·《书名》作者·二维码 | 有成品样张 |
| 经典 | 深底·头像+昵称+摘录日期·暖米色正文·/书名·章节·分割线 | 有成品样张 |
| 锦书 | 粉底·书名大标题置顶·作者·正文·/章节·页脚昵称日期 | 有成品样张 |
| 墨白 | 深炭底暖米字·顶底做旧粗横杠·书名大标题·中文数字日期·无 QR | ✅ 已校准 |
| 手札 | 深炭底暖米字·内嵌信笺细边框·头像头带分隔·无 QR | ✅ 已校准 |
| 卡片(渐变) | 渐变外框+深色圆角卡·头像+日期·/书名\|章节·水印+二维码 | 来自 ifenxiang，可自选 |

## 推进路径

- **Phase 0 打地基**：拷 squoosh 的 Tauri+Vite 骨架 → `Renderer` 接口 → 单主题(经典)跑通「输入→预览→导出 PNG」→ **WKWebView 保真验证**（生死判官）
- **Phase 1 主题矩阵**：补齐主题 + 3 系统字体 + 背景系统
- **Phase 2 打磨**：自适应字号、倍率导出(2x/4x/6x)、剪贴板、拖拽头像、URL 配置持久化
- **Phase 3（可选）**：批量队列(复用 squoosh)、剪贴板/微读文本导入解析

## 开放项 / 待补

- 墨白/手札 已按样张校准完成。刻意简化：墨白粗杠取实色(非做旧纹理)
- 字体已从"模拟微信读书三档"改为直选 11 档系统字体真名(见字体映射一节)；如需更贴近微读楷体的个性，可视需要单点内嵌霞鹜文楷(LXGW WenKai, OFL)
- 首版默认「单卡手动录入」（对齐 app.webp）；批量与导入放 Phase 3
